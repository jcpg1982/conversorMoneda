<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.com.master.machines.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestBCP"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.activities.loadingActivity.LoadingActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TestBCP.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.mainActivity.MainActivity"
            android:theme="@style/Theme.TestBCP.NoActionBar" />


        <activity
            android:name=".ui.activities.activityListMoney.ActivityListMoney"
            android:theme="@style/Theme.TestBCP.NoActionBar" />

    </application>

</manifest>